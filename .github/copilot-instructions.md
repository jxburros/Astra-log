Follow `AGENTS.md` before making any changes.

Before editing code, read:
- all `coreIdentity` files
- the `productRoadmap` document
- `README.md`
- the existing changelog, if present

When given multiple tasks, complete as many as possible, preferably all.

After making changes:
- update or create the changelog
- append a new entry without deleting previous entries
- include the AI/tool name in the changelog entry
- update `README.md`
- update any other relevant documentation

Keep changes aligned with the product direction and app identity.
Prefer focused, safe diffs over unnecessary refactors.
